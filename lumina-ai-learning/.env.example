
# Google Gemini API Key
# For local development (client-exposed Vite variable - not for production):
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# For production on Vercel (server-only variable - recommended):
# Set this in Vercel Dashboard as `GEMINI_API_KEY` (do NOT prefix with VITE_)
GEMINI_API_KEY=your_gemini_api_key_here (server-only)

# Optional: Add other API keys or configuration here
# VITE_ANALYTICS_ID=your_analytics_id

